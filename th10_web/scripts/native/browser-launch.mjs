import {chromium} from 'playwright';import {existsSync,readdirSync} from 'node:fs';import {resolve} from 'node:path';import {homedir} from 'node:os';
function installedChromium(){if(process.env.PLAYWRIGHT_CHROMIUM_EXECUTABLE)return process.env.PLAYWRIGHT_CHROMIUM_EXECUTABLE;const expected=chromium.executablePath();if(existsSync(expected))return expected;const cache=process.env.PLAYWRIGHT_BROWSERS_PATH??(process.platform==='win32'?resolve(process.env.LOCALAPPDATA??resolve(homedir(),'AppData/Local'),'ms-playwright'):process.platform==='darwin'?resolve(homedir(),'Library/Caches/ms-playwright'):resolve(homedir(),'.cache/ms-playwright'));if(!existsSync(cache))return undefined;const suffix=process.platform==='win32'?'chrome-headless-shell-win64/chrome-headless-shell.exe':process.platform==='darwin'?`chrome-headless-shell-mac-${process.arch==='arm64'?'arm64':'x64'}/chrome-headless-shell`:'chrome-headless-shell-linux64/chrome-headless-shell';return readdirSync(cache).filter(name=>/^chromium_headless_shell-\d+$/.test(name)).sort((a,b)=>Number(b.split('-').pop())-Number(a.split('-').pop())).map(name=>resolve(cache,name,suffix)).find(existsSync);}
export function launchBrowser(options={}){const hardware=process.env.NATIVE_GPU==='1'||process.env.PIXEL_GPU==='1';return chromium.launch({headless:true,executablePath:installedChromium(),args:hardware?['--enable-gpu','--use-gl=angle','--use-angle=d3d11']:['--enable-unsafe-swiftshader'],...options});}
