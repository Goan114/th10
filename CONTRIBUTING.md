# 贡献指南

[简体中文](CONTRIBUTING.md) | [English](CONTRIBUTING.en.md)

感谢你愿意帮助这个项目。最有价值的贡献不是扩大声明，而是让一个具体行为更可读、更可移植或更可验证。

## 适合贡献的方向

- 可复现的兼容性问题与真实录像结果；
- 浏览器、操作系统、GPU、音频或输入设备适配；
- 不改变游戏规则的性能与可维护性改进；
- 自动化测试、验证工具和证据整理；
- 文档、翻译和无障碍输入改进。

## 开始前

1. 阅读 `README.zh-CN.md`、`docs/NATIVE-APPLICATION-VALIDATION.md` 和相关源码目录。
2. 先搜索现有实现，复用项目已有抽象，不为单一录像或设备加入特殊分支。
3. 明确修改影响的是游戏逻辑、平台连接还是浏览器宿主。
4. 不提交原版 EXE、DLL、私有资源、凭据、完整外部录像集或无法说明来源的代码。

## 行为修改需要的证据

提交说明至少应包含：

- 复现条件：版本、语言、机体、难度、关卡、浏览器或设备；
- 修改前后的可观察差异；
- 使用的录像、输入序列或测试场景；
- 实际运行的验证命令和结果；
- 尚未覆盖的边界。

请区分以下证据，不要互相替代：

| 证据 | 能说明什么 |
| --- | --- |
| `npm run verify` | 当前源码、模块和发布资源内部一致 |
| 自动单元或回归测试 | 指定场景符合测试契约 |
| 原版逐指令或逐帧对照 | 指定输入和观测字段与参考实现一致 |
| 完整录像回放 | 真实长输入能走到预期结局并得到对应状态 |
| 浏览器或实体设备测试 | 指定环境中的画面、声音、输入和性能可用 |

## 代码约束

- C++ 目标为 C++17；当前 Wasm 构建关闭异常和 RTTI。
- 保持固定时序、录像格式、存档格式和原始资源解释语义。
- 平台特性放入 `source/cpp/platform` 或 `site/runtime`，不要渗入通用游戏规则。
- 修复被验证无效时，应撤回该尝试，不留下无法解释的兼容分支。
- 新增或更换第三方代码时，记录固定版本、来源、许可和必要的校验值。

## 建议验证流程

```powershell
.\tools\node.exe scripts\verify.mjs
```

修改 C++ 后还应使用 WASI SDK 34 重新构建，再执行 `verify`。若修改会影响行为，补充相应回归或录像记录；不要把仅编译通过描述为行为验证完成。

## 语言与文档

面向用户的核心文档同时维护简体中文和英文。术语、数字、命令和证据边界应在两种版本中一致。纯翻译提交不需要重跑游戏，但应检查链接、表格和命令是否保持原意。

## 许可边界

项目自有代码沿用项目已经确定的许可证。该许可证不覆盖原作、汉化资源或第三方组件；贡献不得改变这些内容各自的权利归属，也不得提交只有贡献者持有使用权、却无权再分发的资源。
